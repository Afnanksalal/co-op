# Release installers

Windows NSIS (`.exe`) and MSI packages for Co-Op Desktop are stored under version folders when a maintainer cuts a production release. GitHub Releases remains the public download source.

Checksums for each version are in `SHA256SUMS.txt`.
